import time


def run_mining_or_infer(
    work_dir: str,
    mir_root: str,
    config_file: str,
    # executor: str,
    # executant_name: str,
    run_as_root: bool,
    # task_id: str = f"task-{time.time()}",
    # run_infer: bool = False,
    # run_mining: bool = False,
) -> int:
    return 0
