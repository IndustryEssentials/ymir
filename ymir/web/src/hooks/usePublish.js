const useMessage = () => {}

const createIframe = () => {

}

const usePublish = () => {

  const callback = () => {

  }
  const publish = (data, callback = () => {}) => {
    // todo
    // create iframe
    // const iframe = createIframe()

    // // send message
    // // callback
    // const message = useMessage(iframe)
    // message.post(data, callback)
  }

  return publish
}

export default usePublish
